"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const util_1 = require("../../../lib/util");
exports.default = (config, context) => {
    const { resource } = context.options;
    if (context.options.target === 'web') {
        Object
            .keys(resource)
            .forEach((name) => {
            const { entry, } = resource[name];
            const entries = (0, util_1.isArray)(entry) ? entry : [entry];
            if (entry) {
                config
                    .entry(name)
                    .merge(entries.map(path => (0, util_1.getAbsolutePath)(context.workDir, path)));
            }
        });
    }
    if (context.options.target === 'lib') {
        const entryKey = Object.keys(context.options.resource)[0];
        const libEntry = require.resolve('../../../lib/lib-entry.js');
        config
            .entry(entryKey)
            .merge([libEntry]);
    }
};
