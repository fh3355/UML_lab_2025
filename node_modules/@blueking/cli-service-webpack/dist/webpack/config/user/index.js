"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyUserConfig = exports.loadUserConfig = void 0;
const tslib_1 = require("tslib");
const path_1 = tslib_1.__importDefault(require("path"));
const fs_1 = tslib_1.__importDefault(require("fs"));
const dotenv_1 = tslib_1.__importDefault(require("dotenv"));
const dotenv_expand_1 = tslib_1.__importDefault(require("dotenv-expand"));
const webpack_chain_1 = tslib_1.__importDefault(require("webpack-chain"));
const util_1 = require("../../../lib/util");
const validate_1 = require("../../../lib/validate");
const modifyDefaultConfig = (context, localConfig) => {
    if (!!context.options.replaceStatic && context.mode === 'production') {
        if (context.options.replaceStatic === true) {
            context.options.replaceStatic = {
                key: '{{ BK_STATIC_URL }}',
            };
        }
        if (typeof context.options.replaceStatic === 'object' && !context.options.replaceStatic.key) {
            context.options.replaceStatic.key = '{{ BK_STATIC_URL }}';
        }
        if (!localConfig.publicPath) {
            context.options.publicPath = context.options.replaceStatic.key;
        }
    }
    if (!context.options.publicPath) {
        context.options.publicPath = '/';
    }
    if (context.options.target === 'web' && !(localConfig === null || localConfig === void 0 ? void 0 : localConfig.copy)) {
        context.options.copy[0].from = context.options.assetsDir;
        context.options.copy[0].to = (0, util_1.getAbsolutePath)(context.workDir, context.options.outputDir, context.options.outputAssetsDirName);
    }
    if ((0, util_1.isObject)(localConfig === null || localConfig === void 0 ? void 0 : localConfig.copy)) {
        context.options.copy = [localConfig === null || localConfig === void 0 ? void 0 : localConfig.copy];
    }
    if (context.options.target === 'lib') {
        context.options.outputAssetsDirName = '';
    }
};
const loadEnv = (workDir, fileName, config = {}) => {
    const filePath = path_1.default.resolve(workDir, fileName);
    if (fileName && fs_1.default.existsSync(filePath)) {
        const env = dotenv_1.default.config({
            path: filePath,
        });
        return dotenv_expand_1.default.expand(Object.assign(Object.assign({}, config), env));
    }
    return {};
};
const loadUserConfig = (_, context) => {
    const localConfigPath = path_1.default.resolve(context.workDir, 'bk.config.js');
    if (process.env.NODE_ENV === 'development') {
        loadEnv(context.workDir, '.bk.local.env');
    }
    if (process.env.BKPAAS_ENVIRONMENT === 'stag') {
        loadEnv(context.workDir, '.bk.stag.env');
    }
    loadEnv(context.workDir, `.bk.${context.mode}.env`);
    loadEnv(context.workDir, '.bk.env');
    if (fs_1.default.existsSync(localConfigPath)) {
        const localConfig = require(localConfigPath);
        (0, validate_1.validate)(localConfig);
        const env = loadEnv(context.workDir, (localConfig === null || localConfig === void 0 ? void 0 : localConfig.customEnv) || '', { ignoreProcessEnv: true });
        Object.keys(env.parsed || {}).forEach((key) => {
            var _a;
            process.env[key] = (_a = env.parsed) === null || _a === void 0 ? void 0 : _a[key];
        });
        if ((0, util_1.isFunction)(localConfig.configureWebpack)) {
            localConfig.configureWebpack = localConfig.configureWebpack(context);
        }
        context.options = Object.assign({}, context.options, localConfig);
        modifyDefaultConfig(context, localConfig);
    }
};
exports.loadUserConfig = loadUserConfig;
const applyUserConfig = (config, context) => {
    config.merge(context.options.configureWebpack);
    const finallyConfig = context.options.chainWebpack(config);
    if (!(finallyConfig instanceof webpack_chain_1.default)) {
        const { log } = require('@blueking/cli-utils');
        log.error('\nbk.config.js 文件配置有误：\n    chainWebpack 方法需要返回一个 webpack-chain 对象，请修改后重试\n');
        process.exit(0);
    }
    return finallyConfig;
};
exports.applyUserConfig = applyUserConfig;
