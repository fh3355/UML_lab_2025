"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = (config, { options, mode, workDir }) => {
    config.when(mode === 'development', () => {
        const path = require('path');
        config.devServer
            .host(options.host)
            .port(options.port)
            .https(options.https)
            .open(options.open)
            .hot(true)
            .compress(true)
            .historyApiFallback(true)
            .set('allowedHosts', 'all')
            .set('static', {
            directory: path.join(workDir, options.assetsDir),
            publicPath: options.publicPath,
            watch: true,
        })
            .set('client', {
            logging: 'error',
            overlay: {
                errors: true,
                warnings: false,
            },
            progress: true,
            reconnect: true,
        });
    });
};
