"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const util_1 = require("../../../lib/util");
exports.default = (config, context) => {
    config.when((context.options.target === 'web'), () => {
        const HtmlWebpackPlugin = require('html-webpack-plugin');
        const { resource } = context.options;
        Object
            .keys(resource)
            .forEach((name) => {
            const htmlPluginOptions = resource[name].html;
            if (htmlPluginOptions) {
                htmlPluginOptions.chunks = (0, util_1.isEmpty)(htmlPluginOptions.chunks) ? [name] : htmlPluginOptions.chunks;
                config
                    .plugin(`html-${name}`)
                    .use(HtmlWebpackPlugin, [htmlPluginOptions]);
            }
        });
    });
};
