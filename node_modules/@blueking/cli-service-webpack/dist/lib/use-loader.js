"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.loaderChain = exports.excludeNodeModules = exports.genTsLoader = exports.genBabelLoader = exports.genThreadLoader = exports.warmupWorker = void 0;
const thread_loader_1 = require("thread-loader");
const util_1 = require("./util");
const threadLoaderPath = require.resolve('thread-loader');
const babelLoaderPath = require.resolve('babel-loader');
const tsLoaderPath = require.resolve('ts-loader');
const vueVersion = (0, util_1.getVueVersion)();
const babelLoaderOptions = {
    exclude: (file) => (/node_modules/.test(file)
        && !/\.vue\.js/.test(file)),
};
const tsLoaderOptions = {
    transpileOnly: true,
    happyPackMode: true,
};
const genThreadLoaderOptions = (parallel) => {
    const threadLoaderOptions = {
        workerParallelJobs: 50,
        poolRespawn: false,
        poolTimeout: 2000,
        poolParallelJobs: 50,
    };
    if (parallel !== true) {
        threadLoaderOptions.workers = parallel === false ? 1 : parallel;
    }
    return threadLoaderOptions;
};
const warmupWorker = (__, context) => {
    (0, thread_loader_1.warmup)(genThreadLoaderOptions(context.options.parallel), [
        vueVersion === 2 ? 'vue-loader-bk' : 'vue-loader',
        'babel-loader',
        'ts-loader',
    ]);
};
exports.warmupWorker = warmupWorker;
const genThreadLoader = (rule, options) => rule
    .use('thread-loader')
    .loader(threadLoaderPath)
    .options(genThreadLoaderOptions(options.parallel))
    .end();
exports.genThreadLoader = genThreadLoader;
const genBabelLoader = (rule) => rule
    .use('babel-loader')
    .loader(babelLoaderPath)
    .options(babelLoaderOptions)
    .end();
exports.genBabelLoader = genBabelLoader;
const genTsLoader = (rule, options) => rule
    .use('ts-loader')
    .loader(tsLoaderPath)
    .options(Object.assign(Object.assign({}, tsLoaderOptions), options))
    .end();
exports.genTsLoader = genTsLoader;
const excludeNodeModules = (rule) => rule.exclude
    .add(/node_modules/)
    .end();
exports.excludeNodeModules = excludeNodeModules;
const loaderChain = (rule, loaders) => {
    let finalRule = rule;
    loaders.forEach((loader) => {
        if ((0, util_1.isFunction)(loader)) {
            finalRule = loader(finalRule);
        }
        else {
            finalRule = loader.loaderFn(finalRule, loader.options);
        }
    });
    return finalRule;
};
exports.loaderChain = loaderChain;
